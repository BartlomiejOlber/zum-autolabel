# use one
data <- read.table("ring.dat", header=FALSE, skip=25)
# data <- read.table("winequality-white.dat", header=FALSE, skip=16, sep=',')
# data <- read.table("satimage.dat", header=FALSE, skip=41)
